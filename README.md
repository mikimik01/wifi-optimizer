# Introduction
This C script in C adjusts the battery power given to the network interface. When the wireless internet source is far away, the power given to the interface will increase, otherwise - decrease.

# Compile & run
The most common: "gcc main.c -o main && ./main"
